import TransportationScreen from "@/components/screen/transportationScreen/transportationScreen";

type Props = {};

const TransportationPage = (props: Props) => {
	return (
		<div>
			<TransportationScreen />
		</div>
	);
};

export default TransportationPage;
