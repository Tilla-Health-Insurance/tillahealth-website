import HomeScreen from "@/components/screen/landingScreen/landingScreen";

type Props = {};

const Landingpage = (props: Props) => {
	return (
		<div>
			<HomeScreen />
		</div>
	);
};

export default Landingpage;
